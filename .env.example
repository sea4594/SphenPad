# Optional but recommended for production static hosting.
VITE_SUDOKUPAD_PROXY_BASE=https://sudokupad-proxy.example.workers.dev

# Cloudflare Worker environment variables (set on the Worker, not in Vite):
# ALLOWED_ORIGIN=https://your-sphenpad.example
# UPSTREAM_TIMEOUT_MS=8000
# MAX_ASSET_BYTES=16777216
